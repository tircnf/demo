<!DOCTYPE html>
<html>
  <head>
    <title><%= pkg.name %></title>
    <base href="index.html">

    <link rel="stylesheet" type="text/css" href="<%= css %>" media="all" />
<style>
/* for ui-sref main page.  add a nice color for the active one.
   stuff here so I dont mistakenly leave it in style.css
*/

.active {
    background-color: green;
}

</style>


  </head>
  <body>

      <div ng-app="app" ng-controller="ctrl" ng-cloak>

          <img ng-show="user.loading" ng-src="img/ajax-loader-kitscanner2.gif">

          <div ng-show="!user">
            You need to login.
            <br>
            <input ng-model="userlogin.username">
            <br>
            <input type="passwword" ng-model="userlogin.password">
            <br>
            <button ng-click="login(userlogin)">LOGIN</button>
            <br> {{loginMessage}}
          </div>

          <div ng-hide="user.loading || !user">

              <mike>
                this will disappear.  the 'mike' element doesn't have a transclude
              </mike>

              <br>

              You will be logged out in {{timeLeft}}

              Choose:

              <a ui-sref="index" ui-sref-active='active'>Index</a>
              <a ui-sref="books" ui-sref-active='active'>Books</a>
              <a href="#" ng-click="logout()"> logout </a>

              <div ui-view></div>

              </div>


      </div>
    <script type="text/javascript" src="<%= js %>"></script>
  </body>
</html>
